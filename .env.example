TELEGRAM_BOT_TOKEN=
SESSION_SECRET=
DATABASE_PATH=./data/bookmarks.db
UPLOADS_PATH=./data/uploads
LOGIN_CODE_TTL_SECONDS=300
LOGIN_CODE_LENGTH=6

# Optional: powers /api/reminder, used by the separate MyReminder Android app
# to sync its text with the first sticky note. Leave both unset to disable it.
# Generate a key with: python -c "import secrets; print(secrets.token_hex(32))"
REMINDER_API_KEY=
REMINDER_API_USERNAME=juan
